server:
  port: 8081

spring:
  profiles:
    active: dev

  application:
    name: AUTH-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/agniBank
    username: root
    password: India@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
